/**
 * 영문 콘텐츠 — 번역 대기 중.
 *
 * 지금은 한국어 콘텐츠를 그대로 참조합니다.
 * `EN_ENABLED`(src/lib/i18n.ts)가 false라 영문 경로가 생성되지 않고
 * 언어 토글도 노출되지 않으므로, 반쯤 번역된 화면이 공개될 일은 없습니다.
 *
 * ── 작업 순서 ─────────────────────────────────────────────
 * 1. 한국어 사이트를 확정한다 (대표 사례·사진 등 실제 자료 반영 후)
 * 2. 이 파일의 문자열을 영문으로 교체한다
 * 3. 대표님 검수를 받는다  ← 통번역 회사이므로 필수
 * 4. i18n.ts 의 EN_ENABLED 를 true 로 바꾼다
 * ──────────────────────────────────────────────────────────
 *
 * 한국어를 먼저 확정하는 이유: 자료가 들어오면 문구가 바뀌므로,
 * 지금 번역하면 같은 내용을 두 번 옮기게 됩니다.
 */

import { ko } from "./ko";
import type { SiteContent } from "./types";

export const en: SiteContent = ko;
